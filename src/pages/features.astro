---
import Layout from '../layouts/Layout.astro';
import Nav from '../components/Nav.astro';
import Footer from '../components/Footer.astro';
---

<Layout
  title="Bill Splitter Features — Fair, Proportional & Equal Splits | BillSplit Calcs"
  description="See everything the BillSplit Calcs bill splitter can do: equal, percentage, and share-based splits, 150+ currencies, optimized settlements, and shareable links — all free."
  keywords="bill splitter, fair bill split calculator, equitable bill split calculator, proportional bill split calculator, group bill split calculator, online bill split calculator, bill split calculator online"
>
  <Nav currentPath="/features" />

  <!-- Features section -->
  <section class="bg-(--color-canvas-soft) py-24">
    <div class="max-w-[1400px] mx-auto px-6">
      <div class="text-center mb-14">
        <div class="font-mono text-xs uppercase tracking-widest text-(--color-mute) mb-3">Features</div>
        <h2 class="text-4xl font-semibold text-(--color-ink) tracking-[-1.28px] leading-tight mb-4">
          Everything you need to settle up.
        </h2>
        <p class="text-lg text-(--color-body) max-w-xl mx-auto">
          Designed to handle the complexity of group expenses without the complexity of a spreadsheet.
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
        <div class="bg-(--color-canvas) rounded-lg p-6 shadow-l2">
          <div class="w-8 h-8 rounded-md bg-(--color-canvas-soft-2) text-(--color-ink) flex items-center justify-center mb-4">
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><circle cx="8" cy="8" r="6" stroke="currentColor" stroke-width="1.5"/><path d="M8 5v3l2 2" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/></svg>
          </div>
          <h3 class="text-base font-semibold text-(--color-ink) mb-2 tracking-tight">Instant calculations.</h3>
          <p class="text-sm text-(--color-body) leading-relaxed">Results update in real-time as you add expenses. No submit button, no waiting.</p>
        </div>

        <div class="bg-(--color-canvas) rounded-lg p-6 shadow-l2">
          <div class="w-8 h-8 rounded-md bg-(--color-canvas-soft-2) text-(--color-ink) flex items-center justify-center mb-4">
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><path d="M8 2v12M2 8h12" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/></svg>
          </div>
          <h3 class="text-base font-semibold text-(--color-ink) mb-2 tracking-tight">150+ currencies.</h3>
          <p class="text-sm text-(--color-body) leading-relaxed">From USD to JPY to NGN. Pick your currency and it's applied everywhere automatically.</p>
        </div>

        <div class="bg-(--color-canvas) rounded-lg p-6 shadow-l2">
          <div class="w-8 h-8 rounded-md bg-(--color-canvas-soft-2) text-(--color-ink) flex items-center justify-center mb-4">
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><path d="M2 4h12M2 8h8M2 12h10" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/></svg>
          </div>
          <h3 class="text-base font-semibold text-(--color-ink) mb-2 tracking-tight">Flexible split types.</h3>
          <p class="text-sm text-(--color-body) leading-relaxed">Split equally, by percentage, or by weighted shares. One expense, infinite ways to be fair.</p>
        </div>

        <div class="bg-(--color-canvas) rounded-lg p-6 shadow-l2">
          <div class="w-8 h-8 rounded-md bg-(--color-canvas-soft-2) text-(--color-ink) flex items-center justify-center mb-4">
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><circle cx="5" cy="5" r="2" stroke="currentColor" stroke-width="1.5"/><circle cx="11" cy="5" r="2" stroke="currentColor" stroke-width="1.5"/><circle cx="8" cy="11" r="2" stroke="currentColor" stroke-width="1.5"/><path d="M7 6l-1.5 3M9 6l1.5 3" stroke="currentColor" stroke-width="1.2" stroke-linecap="round"/></svg>
          </div>
          <h3 class="text-base font-semibold text-(--color-ink) mb-2 tracking-tight">Optimized settlements.</h3>
          <p class="text-sm text-(--color-body) leading-relaxed">Our algorithm minimizes total transactions. Fewer payments, faster settlement for everyone.</p>
        </div>

        <div class="bg-(--color-canvas) rounded-lg p-6 shadow-l2">
          <div class="w-8 h-8 rounded-md bg-(--color-canvas-soft-2) text-(--color-ink) flex items-center justify-center mb-4">
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><path d="M10 6L14 8L10 10" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/><path d="M14 8H6C4.343 8 3 6.657 3 5V5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/></svg>
          </div>
          <h3 class="text-base font-semibold text-(--color-ink) mb-2 tracking-tight">Shareable links.</h3>
          <p class="text-sm text-(--color-body) leading-relaxed">Your bill is encoded in the URL. Share via any messenger — everyone sees the exact same data.</p>
        </div>

        <div class="bg-(--color-canvas) rounded-lg p-6 shadow-l2">
          <div class="w-8 h-8 rounded-md bg-(--color-canvas-soft-2) text-(--color-ink) flex items-center justify-center mb-4">
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><rect x="2" y="4" width="12" height="8" rx="2" stroke="currentColor" stroke-width="1.5"/><path d="M2 7h12" stroke="currentColor" stroke-width="1.5"/></svg>
          </div>
          <h3 class="text-base font-semibold text-(--color-ink) mb-2 tracking-tight">Multi-language UI.</h3>
          <p class="text-sm text-(--color-body) leading-relaxed">Switch the interface to your preferred language. 10+ languages with RTL support included.</p>
        </div>
      </div>
    </div>
  </section>

  <Footer />
</Layout>
